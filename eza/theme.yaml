filekinds:
  executable: { is_bold: false }
  socket: { foreground: Magenta, is_bold: false }

symlink_path: { foreground: LightGray }

file_type:
  image: { foreground: Default }
  video: { foreground: Default, is_bold: false }
  music: { foreground: Default }
  lossless: { foreground: Default, is_bold: false }
  crypto: { foreground: Default, is_bold: false }
  document: { foreground: Default }
  compressed: { foreground: Default }
  temp: { foreground: Default }
  compiled: { foreground: Default }
  build: { foreground: Default, is_bold: false, is_underline: false }
  source: { foreground: Default, is_bold: false }

size:
  number_kilo: { foreground: "LightGray", is_bold: false }
  number_byte: { foreground: "LightGray" }
  number_mega: { foreground: "LightGray" }
  number_giga: { foreground: "LightGray" }
  number_huge: { foreground: "LightGray" }

punctuation: { foreground: LightGray, is_bold: false }

date: { foreground: LightGray }

header: { is_bold: true, is_underline: false }
